package fr.syzonia.core.coins;

import org.bukkit.entity.Player;

import fr.syzonia.syzodb.mysql.PlayerInfo;


public class CoinsUtils {

	public static String playerCoinsLevel(Player player) {
  		PlayerInfo playerinfo = new PlayerInfo(player.getUniqueId());
  		long Milliard = 1000000000;
  		
  		if(playerinfo.getCoinsNumber() < 10000000) {
  			return "" + playerinfo.getCoinsNumber() + "";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  10000000 && playerinfo.getCoinsNumber() < 20000000) {
  			return "10mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  20000000 && playerinfo.getCoinsNumber() < 30000000) {
  			return "20mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  30000000 && playerinfo.getCoinsNumber() < 40000000) {
  			return "30mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  40000000 && playerinfo.getCoinsNumber() < 50000000) {
  			return "40mil";
  		}
  
  		if(playerinfo.getCoinsNumber() ==  50000000 && playerinfo.getCoinsNumber() < 60000000) {
  			return "50mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  60000000 && playerinfo.getCoinsNumber() < 70000000) {
  			return "60mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  70000000 && playerinfo.getCoinsNumber() < 80000000) {
  			return "70mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  80000000 && playerinfo.getCoinsNumber() < 90000000) {
  			return "80mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  90000000 && playerinfo.getCoinsNumber() < 100000000 ) {
  			return "90mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  100000000 && playerinfo.getCoinsNumber() < 200000000) {
  			return "100mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  200000000 && playerinfo.getCoinsNumber() < 300000000) {
  			return "200mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  300000000 && playerinfo.getCoinsNumber() < 400000000) {
  			return "300mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  400000000 && playerinfo.getCoinsNumber() < 500000000) {
  			return "400mil";
  		}
  				
  		if(playerinfo.getCoinsNumber() ==  500000000 && playerinfo.getCoinsNumber() < 600000000) {
  			return "500mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  600000000 && playerinfo.getCoinsNumber() < 700000000) {
  			return "600mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  700000000 && playerinfo.getCoinsNumber() < 800000000) {
  			return "700mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  800000000 && playerinfo.getCoinsNumber() < 900000000) {
  			return "800mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  900000000 && playerinfo.getCoinsNumber() < 1000000000) {
  			return "900mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 1 && playerinfo.getCoinsNumber() < Milliard * 2) {
  			return "1milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 2 && playerinfo.getCoinsNumber() < Milliard * 3) {
  			return "2milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 3 && playerinfo.getCoinsNumber() < Milliard * 4) {
  			return "3milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 4 && playerinfo.getCoinsNumber() < Milliard * 5) {
  			return "4milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 5 && playerinfo.getCoinsNumber() < Milliard * 6) {
  			return "5milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 6 && playerinfo.getCoinsNumber() < Milliard * 7) {
  			return "6milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 7 && playerinfo.getCoinsNumber() < Milliard * 8) {
  			return "7milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 8 && playerinfo.getCoinsNumber() < Milliard * 9) {
  			return "8milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() ==  Milliard * 9 && playerinfo.getCoinsNumber() < Milliard * 10) {
  			return "9milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() == Milliard * 10) {
  			return "10md";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  10000000 && playerinfo.getCoinsNumber() < 20000000) {
  			return "+10mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  20000000 && playerinfo.getCoinsNumber() < 30000000) {
  			return "+20mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  30000000 && playerinfo.getCoinsNumber() < 40000000) {
  			return "+30mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  40000000 && playerinfo.getCoinsNumber() < 50000000) {
  			return "+40mil";
  		}
  
  		if(playerinfo.getCoinsNumber() > 50000000 && playerinfo.getCoinsNumber() < 60000000) {
  			return "+50mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  60000000 && playerinfo.getCoinsNumber() < 70000000) {
  			return "+60mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  70000000 && playerinfo.getCoinsNumber() < 80000000) {
  			return "+70mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  80000000 && playerinfo.getCoinsNumber() < 90000000) {
  			return "+80mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  90000000 && playerinfo.getCoinsNumber() < 100000000 ) {
  			return "+90mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 100000000 && playerinfo.getCoinsNumber() < 200000000) {
  			return "+100mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  200000000 && playerinfo.getCoinsNumber() < 300000000) {
  			return "+200mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  300000000 && playerinfo.getCoinsNumber() < 400000000) {
  			return "+300mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 400000000 && playerinfo.getCoinsNumber() < 500000000) {
  			return "+400mil";
  		}
  				
  		if(playerinfo.getCoinsNumber() > 500000000 && playerinfo.getCoinsNumber() < 600000000) {
  			return "+500mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 600000000 && playerinfo.getCoinsNumber() < 700000000) {
  			return "+600mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 700000000 && playerinfo.getCoinsNumber() < 800000000) {
  			return "+700mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 800000000 && playerinfo.getCoinsNumber() < 900000000) {
  			return "+800mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() > 900000000 && playerinfo.getCoinsNumber() < 1000000000) {
  			return "+900mil";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 1 && playerinfo.getCoinsNumber() < Milliard * 2) {
  			return "+1milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 2 && playerinfo.getCoinsNumber() < Milliard * 3) {
  			return "+2milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 3 && playerinfo.getCoinsNumber() < Milliard * 4) {
  			return "+3milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() > Milliard * 4 && playerinfo.getCoinsNumber() < Milliard * 5) {
  			return "+4milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() > Milliard * 5 && playerinfo.getCoinsNumber() < Milliard * 6) {
  			return "+5milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 6 && playerinfo.getCoinsNumber() < Milliard * 7) {
  			return "+6milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 7 && playerinfo.getCoinsNumber() < Milliard * 8) {
  			return "+7milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 8 && playerinfo.getCoinsNumber() < Milliard * 9) {
  			return "+8milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() >  Milliard * 9 && playerinfo.getCoinsNumber() < Milliard * 10) {
  			return "+9milia";
  		}
  		
  		if(playerinfo.getCoinsNumber() > Milliard * 10) {
  			return "+10md";
  		}
		
		
  			
  		return "";
  	}
	
}
